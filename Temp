<?php
$allowedOrigins = array('a.com', 'b.com');
$origin = $_SERVER['HTTP_ORIGIN'];

if (in_array($origin, $allowedOrigins)) {
    // Allow the request
    header('Access-Control-Allow-Origin: ' . $origin);
    // ... Rest of your code here ...
} else {
    // Block the request
    http_response_code(403);
    echo 'Forbidden';
    exit;
}
?>
